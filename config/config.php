<?php
/**
 * پیکربندی اتصال به وب‌سرویس پیام‌رسان.
 *
 * مقادیر از متغیرهای محیطی خوانده می‌شوند، نه از داخل این فایل. کلید API
 * یک راز است: اگر داخل کد بنویسیدش، با اولین push به مخزن می‌رود و از
 * تاریخچهٔ گیت بیرون نمی‌آید.
 *
 * فایل .env.example را کپی کنید به .env و مقادیر خودتان را بگذارید.
 */

return [
    // کلید اختصاصی حساب، از پنل کاربری پیام‌رسان.
    // شکل آن: شش رقم شناسهٔ کاربری، خط تیره، پانزده نویسهٔ حرف و عدد.
    'api_key' => getenv('PAYAM_RESAN_API_KEY') ?: '',

    // یکی از خطوط فعال حساب. با AccountInfo می‌توانید فهرستشان را ببینید.
    'sender' => getenv('PAYAM_RESAN_SENDER') ?: '',

    // در حالت عادی دست نزنید. فقط https، چون کلید در متدهای GET داخل
    // نشانی می‌نشیند و روی http روی شبکه لخت می‌رود.
    'base_url' => getenv('PAYAM_RESAN_BASE_URL') ?: 'https://api.sms-webservice.com/api/V3',
];
